cosmoshub_node:
  api:
    enable: true

  # Pruning configuration
  pruning: "custom"
  pruning_keep_recent: "1000"
  pruning_keep_every: "0"
  pruning_interval: "100"

  tx_index:
    indexer: "kv"

validator:
  enabled: false
  
fullnode:
  enabled: true

