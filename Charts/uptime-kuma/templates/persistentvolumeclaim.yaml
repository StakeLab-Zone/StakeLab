{{- if .Values.local_storage.enabled -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: uptime-kuma-node-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: uptime-kuma-local-storage
  resources:
    requests:
      storage: {{ $.Values.local_storage.capacity }}
{{- end }}
